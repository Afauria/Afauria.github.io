---
layout: post
title: GitHub Pages介绍和使用
date: 2021-10-01
categories: 博客
tags:
- 博客
description: GitHub Pages是GitHub提供的静态站点托管服务，本篇介绍GitHub Pages简单使用，以及如何绑定自定义域名
---

# 前言

本来是在Hexo中介绍如何部署到GitHub Pages，为了避免介绍其他工具的时候重复写，因此单独把GitHub Pages抽出来介绍。

# 简介

GitHub Pages是GitHub提供的免费的静态站点托管服务。通过Hexo、Jekyll灯静态博客生成器生成静态站点文件之后，可以部署到GitHub Pages上。默认使用`github.io`子域名。

GitHub Pages本质就是一个GitHub仓库，只不过GitHub自动将仓库路径下的文件部署到了静态站点服务器`github.io`上，通过特定的url即可访问。（该仓库可以是Private的）

# 使用

1. GitPages创建：登录GitHub，创建一个仓库，命名为：**<用户名>.github.io**。例如**Afauria.github.io**
1. 【Settings-Pages】中选择站点路径
2. 本地生成静态文件
   1. 使用静态站点生成器生成静态博客站点，例如Hexo、Hugo
   2. GitHub内置了Jekyll支持，因此可以直接推送源工程，由GitHub远程构建
   3. 可以本地手写Html前端页面，部署
3. 使用Git命令将静态文件推到GitHub上
4. 访问地址 `https://afauria.github.io/` 。
5. 域名绑定，见下文

![域名添加DNS记录](./2021-10-01-GitPages/GitPages配置.png)

可以看到GitHub Pages提供了两个两个站点路径，一个是根路径，一个是`/docs`路径。意味着我们可以部署两个文件夹到对应的路径下。

我个人是用根路径部署博客的静态站点，用`/docs`路径部署GitBook静态站点。

注意：

1. 使用根路径的时候，GitHub仓库根目录即站点入口。
2. 使用`/docs`的时候，GitHub仓库要创建docs文件夹，将静态站点文件放到该文件夹下，作为站点入口。

# 域名绑定

申请域名：万网、阿里云、腾讯云都可以申请

## 配置DNS解析

以阿里云为例

![域名添加DNS记录](./2021-10-01-GitPages/域名添加DNS记录.png)

## 添加CNAME文件

**注：方式二会在远程仓库生成CNAME文件和新的提交，会产生分支冲突，导致本地发布博客失败，建议使用方式一**

> 产生冲突的时候可以执行`hexo clean`再发布

方式一：在Hexo博客站点`source`目录下添加`CNAME`文件，文件中填写域名地址即可。`hexo clean & hexo g -d`重新发布

方式二：GitPage中配置自定义域名，会生成CNAME文件并发布

![GitPage配置CNAME](./2021-10-01-GitPages/GitPage配置CNAME.png)

# DNS记录类型

域名解析就是域名到IP地址的转换过程。域名的解析工作由DNS服务器完成。

DNS服务器会存储记录，记录类型有多种类型，起到不同的作用，这里简单介绍下A记录、CNAME记录和NS记录

## A记录

Address记录。存储域名到对应IP的记录，将域名解析成IP

## CNAME记录

别名记录，将一个域名解析成另一个域名。

CNAME记录有如下作用

### 将多个域名映射到同一个域名

当需要多个域名指向同一个IP时，如果使用A记录，如下，一旦IP变更，需要修改多条A记录。

> 1. `www.aaa.com-->1.1.1.1`
> 2. `www.bbb.com-->1.1.1.1`

如果使用CNAME记录，如下，只需要修改A记录即可

> 1. `www.aaa.com-->www.xxx.com-->1.1.1.1`
> 2. `www.bbb.com-->www.xxx.com-->1.1.1.1`

### CDN加速

使用CNAME将域名指向CDN服务器，由CDN服务器把最近的（或者负载较低的）服务器IP返回给本地DNS，让客户端能够快速访问。

> CDN（Content Delivery Network，内容分发网络）：自动选择靠近用户的服务器，更快的分发网络资源，并且提供缓存能力。

## NS记录

域名服务器记录。指定域名由哪个DNS服务器解析

## 补充

DNS解析流程：客户端==>本地hosts查询==>ISP本地运营商查询DNS缓存==>权威DNS服务器解析

本地hosts文件：保存常用的网址域名与其对应IP地址的映射。

> 当用户在浏览器中输入一个需要登录的网址域名时，系统会首先自动从hosts文件中寻找对应的IP地址，一旦找到，系统会立即打开对应网页，如果没有找到，则系统会将网址提交DNS服务器进行IP地址的解析，再进行访问。

可以使用`ipconfig /flushdns`命令清除DNS缓存

# 结语

参考资料：[简单的解释下什么是CNAME？](https://blog.csdn.net/DD_orz/article/details/100034049)
